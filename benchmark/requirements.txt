# Python dependencies required by the Julia-vs-QMCPy benchmark workflow.
#
# Keep these pinned so benchmark artifacts remain comparable across repository
# commits instead of silently drifting with upstream Python package releases.
qmctoolscl==1.2.3
-r qmcpy-requirements.txt
