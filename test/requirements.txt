# No additional Python packages are required for QuasiMC.jl's core tests or
# docs after the staged QMCToolsCL_jll migration.
#
# Notebook workflows may still provision a plain Python interpreter as a
# `jupyter` frontend, but that setup is independent from QuasiMC's own runtime
# generator backend. Benchmarking keeps its separate pinned requirements in
# `benchmark/requirements.txt`.
